import 'package:flutter_test/flutter_test.dart';
import 'package:sound_indicator/sound_indicator.dart';

void main() {
  test('adds one to input values', () {
    const calculator = SoundIndicatorWidget();
    expect(calculator, calculator);
  });
}
